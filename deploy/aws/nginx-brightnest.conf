# =============================================
# BrightNest Academy - Production Nginx Config
# Flow: User -> Nginx :443/:80 -> Docker :8080
# SSL via Let's Encrypt / Certbot
# =============================================

# WebSocket upgrade map
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Reusable upstream for app container/service
upstream brightnest_backend {
    server 127.0.0.1:8080 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

# =============================================
# Rate Limiting Zones
# =============================================
limit_req_zone $binary_remote_addr zone=general:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;

# =============================================
# HTTP -> HTTPS Redirect (all traffic)
# =============================================
server {
    listen 80;
    listen [::]:80;
    server_name brightnest-academy.com www.brightnest-academy.com;

    # Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# =============================================
# HTTPS Server Block (Primary)
# =============================================
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name brightnest-academy.com www.brightnest-academy.com;
    absolute_redirect off;
    keepalive_timeout 65s;
    keepalive_requests 1000;
    reset_timedout_connection on;
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;

    # -----------------------------------------
    # SSL/TLS Configuration (Let's Encrypt)
    # -----------------------------------------
    ssl_certificate /etc/letsencrypt/live/brightnest-academy.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/brightnest-academy.com/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/brightnest-academy.com/chain.pem;

    # Modern TLS only (TLS 1.2 + 1.3)
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # OCSP stapling
    ssl_stapling on;
    ssl_stapling_verify on;
    resolver 8.8.8.8 8.8.4.4 valid=300s;
    resolver_timeout 5s;

    # SSL session caching
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    # -----------------------------------------
    # Security Headers (defense-in-depth)
    # -----------------------------------------
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(self)" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Cross-Origin-Resource-Policy "same-origin" always;

    # -----------------------------------------
    # Request Size Limits
    # -----------------------------------------
    client_max_body_size 10M;
    client_body_timeout 30s;
    client_header_timeout 30s;
    send_timeout 30s;
    client_body_buffer_size 32k;
    client_header_buffer_size 4k;
    large_client_header_buffers 4 16k;

    # -----------------------------------------
    # Gzip Compression
    # -----------------------------------------
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 5;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/json
        application/xml
        application/xml+rss
        image/svg+xml
        font/woff2;

    # -----------------------------------------
    # Proxy Buffering (better upstream throughput)
    # -----------------------------------------
    proxy_buffering on;
    proxy_buffer_size 16k;
    proxy_buffers 8 16k;
    proxy_busy_buffers_size 32k;
    proxy_temp_file_write_size 64k;

    # -----------------------------------------
    # Static Assets (cached at Nginx layer)
    # -----------------------------------------
    location ~* \.(css|js)$ {
        proxy_pass http://brightnest_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 15s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        expires 30d;
        add_header Cache-Control "public, max-age=2592000, immutable" always;
        access_log off;
    }

    location ~* \.(png|jpg|jpeg|gif|ico|svg|webp|avif)$ {
        proxy_pass http://brightnest_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 15s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        expires 60d;
        add_header Cache-Control "public, max-age=5184000, immutable" always;
        access_log off;
    }

    location ~* \.(woff|woff2|ttf|eot)$ {
        proxy_pass http://brightnest_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 15s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        expires 60d;
        add_header Cache-Control "public, max-age=5184000, immutable" always;
        access_log off;
    }

    # -----------------------------------------
    # Login endpoint: strict rate limiting
    # -----------------------------------------
    location = /api/auth/login {
        limit_req zone=login burst=3 nodelay;
        limit_req_status 429;

        proxy_pass http://brightnest_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_connect_timeout 15s;
        proxy_send_timeout 45s;
        proxy_read_timeout 45s;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_next_upstream_tries 2;
    }

    # -----------------------------------------
    # API endpoints: moderate rate limiting
    # -----------------------------------------
    location /api/ {
        limit_req zone=api burst=40;
        limit_req_status 429;

        proxy_pass http://brightnest_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        # Prevent buffering for real-time responses
        proxy_buffering off;

        proxy_connect_timeout 15s;
        proxy_send_timeout 90s;
        proxy_read_timeout 90s;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_next_upstream_tries 2;
    }

    # -----------------------------------------
    # Actuator: restrict to localhost only
    # -----------------------------------------
    location /actuator/ {
        allow 127.0.0.1;
        allow ::1;
        deny all;

        proxy_pass http://brightnest_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    # Health check (public)
    location = /health {
        proxy_pass http://brightnest_backend;
        proxy_set_header Host $host;
        access_log off;
    }

    # Canonical home route without upstream scheme rewriting
    location = / {
        return 302 https://$host/index.html;
    }

    # -----------------------------------------
    # Default: general rate limiting + proxy
    # -----------------------------------------
    location / {
        proxy_pass http://brightnest_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_redirect ~^http://([^/]+)(/.*)$ https://$1$2;

        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Timeouts
        proxy_connect_timeout 15s;
        proxy_send_timeout 90s;
        proxy_read_timeout 90s;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_next_upstream_tries 2;
    }

    # -----------------------------------------
    # Block sensitive paths
    # -----------------------------------------
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    location ~* \.(env|git|gitignore|dockerignore|yml|yaml|properties|xml|sql|bak|log)$ {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Hide Nginx version
    server_tokens off;

    # -----------------------------------------
    # Custom Error Pages
    # -----------------------------------------
    error_page 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
        internal;
    }
}
